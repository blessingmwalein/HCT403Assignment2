# ass-front is the front end

## Build Setup

```bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3000
$ npm run dev

#test is the backend
#create database named hct403ass2 in your php my admin and import sql file provided in the project folder named hct403ass2.sql
#configure the database in the tes/app/Config/Database.php

# start the backend run

$ php -s 127.0.0.1:8000
```
